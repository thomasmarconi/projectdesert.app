# For local Docker setup (npm run db:up from frontend), use these values:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/project_desert"
NEXTAUTH_SECRET="<generated-secret>" # openssl rand -base64 32